//
// Created by xande on 9/2/2022.
//
#include <iostream>
#include <fstream>
#include <sstream>
#include "Regex.h"
/* std::string Regex::set_pattern(std::string pat) {
    // set the pattern of the string'
    Regex set = Regex();
    set.pattern = pat;

}; */
    std::string Regex::search(std::string pattern, std::string filedir){\
        /*search for Regex pattern
 *  take in pattern [a-zA-Z0-9_.+-]
        */
    };
    /* void Regex::getstring(std::string file){
        std::string str;
        std::ifstream myfile;
        myfile.open(file);
        if (myfile) {
            std::ostringstream ss;
            ss << myfile.rdbuf();
            str =  ss.str();

        }
        std::cout << str;
        */
    /*void setPattern(std::string pat) {
        
    }
*/

